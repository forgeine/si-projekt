{% extends 'base.html.twig' %}

{% block title %}Edit Profile{% endblock %}

{% block body %}
    <a href="{{ path('profile_index') }}" class="btn btn-warning btn-sm">{{ 'action.profile_index'|trans }}</a>
    <a href="{{ path('profile_password')}}" class="btn btn-primary btn-sm">{{ 'action.change_password'|trans }}</a>
    <h1>Edit Profile</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
{% endblock %}
