{% extends 'base.html.twig' %}

{% block title %}Edit Profile{% endblock %}

{% block body %}
    <h1>Edit Profile</h1>
    <a href="{{ path('profile_index') }}" class="btn btn-outline-primary">{{ 'action.profile_index'|trans }}</a>
    <a href="{{ path('profile_password')}}" class="btn btn-outline-primary">{{ 'action.change_password'|trans }}</a>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
{% endblock %}
